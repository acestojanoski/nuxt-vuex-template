import { setValue } from './set-value';
import { loadContent } from './load-content';
import { nuxtServerInit } from './nuxt-server-init';

const actions = {
    setValue,
    loadContent,
    nuxtServerInit
};

export default actions;
